openapi: 3.1.0
info:
  title: Api
  version: 0.1.0
  description: FSM Platform API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
  - name: auth
  - name: dashboard
  - name: customers
  - name: jobs
  - name: quotes
  - name: invoices
  - name: fleet
  - name: maintenance
  - name: employees
  - name: permissions
  - name: leads

paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema: { $ref: "#/components/schemas/HealthStatus" }

  /auth/login:
    post:
      operationId: login
      tags: [auth]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/LoginRequest" }
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AuthMeResponse" }
        "401": { $ref: "#/components/responses/Error" }

  /auth/logout:
    post:
      operationId: logout
      tags: [auth]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Ok" }

  /auth/me:
    get:
      operationId: getMe
      tags: [auth]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AuthMeResponse" }
        "401": { $ref: "#/components/responses/Error" }

  /admin/dashboard-summary:
    get:
      operationId: getDashboardSummary
      tags: [dashboard]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DashboardSummary" }

  /customers:
    get:
      operationId: listCustomers
      tags: [customers]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CustomerListResponse" }
    post:
      operationId: createCustomer
      tags: [customers]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CustomerWriteBody" }
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CustomerResponse" }
  /customers/{id}:
    get:
      operationId: getCustomerProfile
      tags: [customers]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CustomerProfileResponse" }
        "404": { $ref: "#/components/responses/Error" }
    patch:
      operationId: updateCustomer
      tags: [customers]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CustomerWriteBody" }
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CustomerResponse" }
    delete:
      operationId: deleteCustomer
      tags: [customers]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Ok" }
  /customers/{id}/properties:
    post:
      operationId: createCustomerProperty
      tags: [customers]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/PropertyWriteBody" }
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PropertyResponse" }
        "404": { $ref: "#/components/responses/Error" }

  /jobs:
    get:
      operationId: listJobs
      tags: [jobs]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/JobListResponse" }
    post:
      operationId: createJob
      tags: [jobs]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/JobWriteBody" }
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/JobResponse" }
  /jobs/{id}:
    patch:
      operationId: updateJob
      tags: [jobs]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/JobWriteBody" }
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/JobResponse" }
    delete:
      operationId: deleteJob
      tags: [jobs]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Ok" }

  /quotes:
    get:
      operationId: listQuotes
      tags: [quotes]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuoteListResponse" }
    post:
      operationId: createQuote
      tags: [quotes]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/QuoteWriteBody" }
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuoteResponse" }
  /quotes/{id}:
    patch:
      operationId: updateQuote
      tags: [quotes]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/QuoteWriteBody" }
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuoteResponse" }
    delete:
      operationId: deleteQuote
      tags: [quotes]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Ok" }

  /invoices:
    get:
      operationId: listInvoices
      tags: [invoices]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/InvoiceListResponse" }
    post:
      operationId: createInvoice
      tags: [invoices]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/InvoiceWriteBody" }
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/InvoiceResponse" }
  /invoices/{id}:
    patch:
      operationId: updateInvoice
      tags: [invoices]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/InvoiceWriteBody" }
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/InvoiceResponse" }
    delete:
      operationId: deleteInvoice
      tags: [invoices]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Ok" }

  /trucks:
    get:
      operationId: listTrucks
      tags: [fleet]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TruckListResponse" }
    post:
      operationId: createTruck
      tags: [fleet]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/TruckWriteBody" }
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TruckResponse" }
  /trucks/{id}:
    patch:
      operationId: updateTruck
      tags: [fleet]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/TruckWriteBody" }
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TruckResponse" }
    delete:
      operationId: deleteTruck
      tags: [fleet]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Ok" }

  /equipment:
    get:
      operationId: listEquipment
      tags: [fleet]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EquipmentListResponse" }
    post:
      operationId: createEquipment
      tags: [fleet]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EquipmentWriteBody" }
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EquipmentResponse" }
  /equipment/{id}:
    patch:
      operationId: updateEquipment
      tags: [fleet]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EquipmentWriteBody" }
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EquipmentResponse" }
    delete:
      operationId: deleteEquipment
      tags: [fleet]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Ok" }

  /maintenance-logs:
    get:
      operationId: listMaintenanceLogs
      tags: [maintenance]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MaintenanceLogListResponse" }
    post:
      operationId: createMaintenanceLog
      tags: [maintenance]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/MaintenanceLogWriteBody" }
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MaintenanceLogResponse" }
  /maintenance-logs/{id}:
    patch:
      operationId: updateMaintenanceLog
      tags: [maintenance]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/MaintenanceLogWriteBody" }
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MaintenanceLogResponse" }
    delete:
      operationId: deleteMaintenanceLog
      tags: [maintenance]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Ok" }

  /admin/users:
    get:
      operationId: listEmployees
      tags: [employees]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EmployeeListResponse" }
    post:
      operationId: createEmployee
      tags: [employees]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EmployeeCreateBody" }
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EmployeeResponse" }
  /admin/users/{id}:
    patch:
      operationId: updateEmployee
      tags: [employees]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EmployeeUpdateBody" }
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EmployeeResponse" }
    delete:
      operationId: deleteEmployee
      tags: [employees]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Ok" }

  /admin/roles:
    get:
      operationId: listRoles
      tags: [employees]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/RoleListResponse" }

  /admin/departments:
    get:
      operationId: listDepartments
      tags: [employees]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DepartmentListResponse" }

  /admin/section-permissions:
    get:
      operationId: listSectionPermissions
      tags: [permissions]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SectionPermissionListResponse" }
    post:
      operationId: updateSectionPermission
      tags: [permissions]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SectionPermissionUpdateBody" }
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Ok" }

  /admin/section-permissions/matrix:
    get:
      operationId: getPermissionMatrix
      tags: [permissions]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PermissionMatrixResponse" }

  /leads:
    get:
      operationId: listLeads
      tags: [leads]
      parameters:
        - in: query
          name: status
          required: false
          schema: { type: string }
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/LeadListResponse" }
    post:
      operationId: createLead
      tags: [leads]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/LeadWriteBody" }
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/LeadResponse" }
  /leads/{id}:
    patch:
      operationId: updateLead
      tags: [leads]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/LeadUpdateBody" }
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/LeadResponse" }
  /portal/auth/request-otp:
    post:
      operationId: portalRequestOtp
      tags: [portal]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/PortalRequestOtpInput" }
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PortalRequestOtpResult" }
        "400": { $ref: "#/components/responses/Error" }
        "429": { $ref: "#/components/responses/Error" }

  /portal/auth/verify-otp:
    post:
      operationId: portalVerifyOtp
      tags: [portal]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/PortalVerifyOtpInput" }
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PortalCustomerResponse" }
        "400": { $ref: "#/components/responses/Error" }
        "429": { $ref: "#/components/responses/Error" }

  /portal/auth/logout:
    post:
      operationId: portalLogout
      tags: [portal]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Ok" }

  /portal/me:
    get:
      operationId: portalMe
      tags: [portal]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PortalCustomerResponse" }
        "401": { $ref: "#/components/responses/Error" }

  /portal/properties:
    get:
      operationId: portalListProperties
      tags: [portal]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PortalPropertiesResponse" }
        "401": { $ref: "#/components/responses/Error" }

  /portal/jobs:
    get:
      operationId: portalListJobs
      tags: [portal]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PortalJobsResponse" }
        "401": { $ref: "#/components/responses/Error" }

  /portal/requests:
    get:
      operationId: portalListRequests
      tags: [portal]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PortalRequestListResponse" }
        "401": { $ref: "#/components/responses/Error" }
    post:
      operationId: portalCreateRequest
      tags: [portal]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/PortalCreateRequestInput" }
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PortalRequestResponse" }
        "400": { $ref: "#/components/responses/Error" }
        "401": { $ref: "#/components/responses/Error" }

  /leads/{id}/convert:
    post:
      operationId: convertLeadToQuote
      tags: [leads]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/LeadConvertResponse" }
        "404": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }

components:
  responses:
    Error:
      description: Error
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorBody" }
  schemas:
    HealthStatus:
      type: object
      required: [status]
      properties:
        status: { type: string }
    Ok:
      type: object
      required: [ok]
      properties:
        ok: { type: boolean }
    ErrorBody:
      type: object
      required: [error]
      properties:
        error: { type: string }

    LoginRequest:
      type: object
      required: [email, password]
      properties:
        email: { type: string, format: email }
        password: { type: string }

    AuthUser:
      type: object
      required: [id, email, fullName, role, department]
      properties:
        id: { type: integer }
        email: { type: string }
        fullName: { type: string }
        role: { type: string }
        department: { type: string }
    AuthMeResponse:
      type: object
      required: [user]
      properties:
        user: { $ref: "#/components/schemas/AuthUser" }

    DashboardKpi:
      type: object
      required: [openJobs, scheduledJobs, completedThisMonth, pipelineCents, paidThisMonthCents, outstandingInvoicesCents, fleetActive, fleetInShop, employees]
      properties:
        openJobs: { type: integer }
        scheduledJobs: { type: integer }
        completedThisMonth: { type: integer }
        pipelineCents: { type: integer }
        paidThisMonthCents: { type: integer }
        outstandingInvoicesCents: { type: integer }
        fleetActive: { type: integer }
        fleetInShop: { type: integer }
        employees: { type: integer }
    DashboardJobByStatus:
      type: object
      required: [status, count]
      properties:
        status: { type: string }
        count: { type: integer }
    DashboardRecentJob:
      type: object
      required: [id, status, totalCents]
      properties:
        id: { type: integer }
        status: { type: string }
        totalCents: { type: integer }
        scheduledFor: { type: string, format: date-time, nullable: true }
        notes: { type: string, nullable: true }
    DashboardSummary:
      type: object
      required: [kpi, jobsByStatus, recentJobs, quotePipelineByStage]
      properties:
        kpi: { $ref: "#/components/schemas/DashboardKpi" }
        jobsByStatus:
          type: array
          items: { $ref: "#/components/schemas/DashboardJobByStatus" }
        quotePipelineByStage:
          type: array
          items: { $ref: "#/components/schemas/DashboardJobByStatus" }
        recentJobs:
          type: array
          items: { $ref: "#/components/schemas/DashboardRecentJob" }

    Customer:
      type: object
      required: [id, fullName, ownerUserId, createdAt]
      properties:
        id: { type: integer }
        fullName: { type: string }
        email: { type: string, nullable: true }
        phone: { type: string, nullable: true }
        billingAddress: { type: string, nullable: true }
        ownerUserId: { type: integer }
        createdAt: { type: string, format: date-time }
    CustomerWriteBody:
      type: object
      required: [fullName, ownerUserId]
      properties:
        fullName: { type: string }
        email: { type: string, nullable: true }
        phone: { type: string, nullable: true }
        billingAddress: { type: string, nullable: true }
        ownerUserId: { type: integer }
    CustomerResponse:
      type: object
      required: [customer]
      properties:
        customer: { $ref: "#/components/schemas/Customer" }
    CustomerListResponse:
      type: object
      required: [customers]
      properties:
        customers:
          type: array
          items: { $ref: "#/components/schemas/Customer" }

    Job:
      type: object
      required: [id, propertyId, status, totalCents, createdAt]
      properties:
        id: { type: integer }
        propertyId: { type: integer }
        crewId: { type: integer, nullable: true }
        status: { type: string }
        scheduledFor: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        totalCents: { type: integer }
        notes: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
    JobWriteBody:
      type: object
      required: [propertyId, status, totalCents]
      properties:
        propertyId: { type: integer }
        crewId: { type: integer, nullable: true }
        status: { type: string }
        scheduledFor: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        totalCents: { type: integer }
        notes: { type: string, nullable: true }
    JobResponse:
      type: object
      required: [job]
      properties:
        job: { $ref: "#/components/schemas/Job" }
    JobListResponse:
      type: object
      required: [jobs]
      properties:
        jobs:
          type: array
          items: { $ref: "#/components/schemas/Job" }

    Quote:
      type: object
      required: [id, customerId, ownerUserId, status, subtotalCents, totalCents, createdAt]
      properties:
        id: { type: integer }
        customerId: { type: integer }
        propertyId: { type: integer, nullable: true }
        ownerUserId: { type: integer }
        status: { type: string }
        subtotalCents: { type: integer }
        totalCents: { type: integer }
        sentAt: { type: string, format: date-time, nullable: true }
        decidedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
    QuoteWriteBody:
      type: object
      required: [customerId, ownerUserId, status, subtotalCents, totalCents]
      properties:
        customerId: { type: integer }
        propertyId: { type: integer, nullable: true }
        ownerUserId: { type: integer }
        status: { type: string }
        subtotalCents: { type: integer }
        totalCents: { type: integer }
        sentAt: { type: string, format: date-time, nullable: true }
        decidedAt: { type: string, format: date-time, nullable: true }
    QuoteResponse:
      type: object
      required: [quote]
      properties:
        quote: { $ref: "#/components/schemas/Quote" }
    QuoteListResponse:
      type: object
      required: [quotes]
      properties:
        quotes:
          type: array
          items: { $ref: "#/components/schemas/Quote" }

    Invoice:
      type: object
      required: [id, customerId, status, totalCents]
      properties:
        id: { type: integer }
        jobId: { type: integer, nullable: true }
        customerId: { type: integer }
        status: { type: string }
        totalCents: { type: integer }
        issuedAt: { type: string, format: date-time, nullable: true }
        paidAt: { type: string, format: date-time, nullable: true }
    InvoiceWriteBody:
      type: object
      required: [customerId, status, totalCents]
      properties:
        jobId: { type: integer, nullable: true }
        customerId: { type: integer }
        status: { type: string }
        totalCents: { type: integer }
        issuedAt: { type: string, format: date-time, nullable: true }
        paidAt: { type: string, format: date-time, nullable: true }
    InvoiceResponse:
      type: object
      required: [invoice]
      properties:
        invoice: { $ref: "#/components/schemas/Invoice" }
    InvoiceListResponse:
      type: object
      required: [invoices]
      properties:
        invoices:
          type: array
          items: { $ref: "#/components/schemas/Invoice" }

    Truck:
      type: object
      required: [id, name, status]
      properties:
        id: { type: integer }
        name: { type: string }
        vin: { type: string, nullable: true }
        plate: { type: string, nullable: true }
        status: { type: string }
        assignedCrewId: { type: integer, nullable: true }
    TruckWriteBody:
      type: object
      required: [name, status]
      properties:
        name: { type: string }
        vin: { type: string, nullable: true }
        plate: { type: string, nullable: true }
        status: { type: string }
        assignedCrewId: { type: integer, nullable: true }
    TruckResponse:
      type: object
      required: [truck]
      properties:
        truck: { $ref: "#/components/schemas/Truck" }
    TruckListResponse:
      type: object
      required: [trucks]
      properties:
        trucks:
          type: array
          items: { $ref: "#/components/schemas/Truck" }

    Equipment:
      type: object
      required: [id, name, type, status]
      properties:
        id: { type: integer }
        name: { type: string }
        type: { type: string }
        serial: { type: string, nullable: true }
        status: { type: string }
        assignedTruckId: { type: integer, nullable: true }
    EquipmentWriteBody:
      type: object
      required: [name, type, status]
      properties:
        name: { type: string }
        type: { type: string }
        serial: { type: string, nullable: true }
        status: { type: string }
        assignedTruckId: { type: integer, nullable: true }
    EquipmentResponse:
      type: object
      required: [equipment]
      properties:
        equipment: { $ref: "#/components/schemas/Equipment" }
    EquipmentListResponse:
      type: object
      required: [equipment]
      properties:
        equipment:
          type: array
          items: { $ref: "#/components/schemas/Equipment" }

    MaintenanceLog:
      type: object
      required: [id, kind, description, performedAt, costCents]
      properties:
        id: { type: integer }
        truckId: { type: integer, nullable: true }
        equipmentId: { type: integer, nullable: true }
        kind: { type: string }
        description: { type: string }
        performedByUserId: { type: integer, nullable: true }
        performedAt: { type: string, format: date-time }
        costCents: { type: integer }
    MaintenanceLogWriteBody:
      type: object
      required: [kind, description, costCents]
      properties:
        truckId: { type: integer, nullable: true }
        equipmentId: { type: integer, nullable: true }
        kind: { type: string }
        description: { type: string }
        performedByUserId: { type: integer, nullable: true }
        performedAt: { type: string, format: date-time, nullable: true }
        costCents: { type: integer }
    MaintenanceLogResponse:
      type: object
      required: [log]
      properties:
        log: { $ref: "#/components/schemas/MaintenanceLog" }
    MaintenanceLogListResponse:
      type: object
      required: [logs]
      properties:
        logs:
          type: array
          items: { $ref: "#/components/schemas/MaintenanceLog" }

    Employee:
      type: object
      required: [id, email, fullName, role, department, isActive, createdAt]
      properties:
        id: { type: integer }
        email: { type: string }
        fullName: { type: string }
        role: { type: string }
        roleId: { type: integer }
        department: { type: string }
        departmentId: { type: integer }
        isActive: { type: boolean }
        createdAt: { type: string, format: date-time }
    EmployeeCreateBody:
      type: object
      required: [email, fullName, password, roleId, departmentId]
      properties:
        email: { type: string, format: email }
        fullName: { type: string }
        password: { type: string, minLength: 6 }
        roleId: { type: integer }
        departmentId: { type: integer }
        isActive: { type: boolean }
    EmployeeUpdateBody:
      type: object
      properties:
        email: { type: string }
        fullName: { type: string }
        password: { type: string }
        roleId: { type: integer }
        departmentId: { type: integer }
        isActive: { type: boolean }
    EmployeeResponse:
      type: object
      required: [employee]
      properties:
        employee: { $ref: "#/components/schemas/Employee" }
    EmployeeListResponse:
      type: object
      required: [employees]
      properties:
        employees:
          type: array
          items: { $ref: "#/components/schemas/Employee" }

    Role:
      type: object
      required: [id, key, label]
      properties:
        id: { type: integer }
        key: { type: string }
        label: { type: string }
    RoleListResponse:
      type: object
      required: [roles]
      properties:
        roles:
          type: array
          items: { $ref: "#/components/schemas/Role" }
    Department:
      type: object
      required: [id, key, label]
      properties:
        id: { type: integer }
        key: { type: string }
        label: { type: string }
    DepartmentListResponse:
      type: object
      required: [departments]
      properties:
        departments:
          type: array
          items: { $ref: "#/components/schemas/Department" }

    SectionPermission:
      type: object
      required: [roleKey, sectionKey, canView, canEdit]
      properties:
        id: { type: integer, nullable: true }
        roleKey: { type: string }
        sectionKey: { type: string }
        canView: { type: boolean }
        canEdit: { type: boolean }
        isOverride: { type: boolean }
    SectionPermissionListResponse:
      type: object
      required: [permissions]
      properties:
        permissions:
          type: array
          items: { $ref: "#/components/schemas/SectionPermission" }
    SectionPermissionUpdateBody:
      type: object
      required: [roleKey, sectionKey, canView, canEdit]
      properties:
        roleKey: { type: string }
        sectionKey: { type: string }
        canView: { type: boolean }
        canEdit: { type: boolean }

    PermissionMatrixCell:
      type: object
      required: [roleKey, sectionKey, canView, canEdit, isOverride]
      properties:
        roleKey: { type: string }
        sectionKey: { type: string }
        canView: { type: boolean }
        canEdit: { type: boolean }
        isOverride: { type: boolean }
    PermissionMatrixResponse:
      type: object
      required: [roles, sections, cells]
      properties:
        roles:
          type: array
          items: { $ref: "#/components/schemas/Role" }
        sections:
          type: array
          items: { type: string }
        cells:
          type: array
          items: { $ref: "#/components/schemas/PermissionMatrixCell" }

    LeadService:
      type: string
      description: Mirrors the service catalogue shown on joshuatreeinc.com so leads from the public site / portal map cleanly without translation.
      enum:
        - TREE_REMOVAL
        - TRIMMING_PRUNING
        - MANGROVE_CARE
        - STUMP_GRINDING
        - EMERGENCY_STORM
        - CRANE_ASSISTED
    LeadStatus:
      type: string
      enum:
        - NEW
        - CONTACTED
        - QUOTED
        - CONVERTED
        - DISMISSED
    LeadSource:
      type: string
      enum:
        - PORTAL
        - PHONE
        - WEB
        - WALKIN
    Lead:
      type: object
      required: [id, customerId, service, status, source, createdAt]
      properties:
        id: { type: integer }
        customerId: { type: integer }
        propertyId: { type: integer, nullable: true }
        service: { $ref: "#/components/schemas/LeadService" }
        notes: { type: string, nullable: true }
        preferredWindowStart: { type: string, format: date-time, nullable: true }
        preferredWindowEnd: { type: string, format: date-time, nullable: true }
        status: { $ref: "#/components/schemas/LeadStatus" }
        source: { $ref: "#/components/schemas/LeadSource" }
        convertedQuoteId: { type: integer, nullable: true }
        createdAt: { type: string, format: date-time }
        customerName: { type: string, nullable: true }
        propertyAddress: { type: string, nullable: true }
    LeadWriteBody:
      type: object
      required: [customerId, service]
      properties:
        customerId: { type: integer }
        propertyId: { type: integer, nullable: true }
        service: { $ref: "#/components/schemas/LeadService" }
        notes: { type: string, nullable: true }
        preferredWindowStart: { type: string, format: date-time, nullable: true }
        preferredWindowEnd: { type: string, format: date-time, nullable: true }
        status: { $ref: "#/components/schemas/LeadStatus" }
        source: { $ref: "#/components/schemas/LeadSource" }
    LeadUpdateBody:
      type: object
      properties:
        propertyId: { type: integer, nullable: true }
        service: { $ref: "#/components/schemas/LeadService" }
        notes: { type: string, nullable: true }
        preferredWindowStart: { type: string, format: date-time, nullable: true }
        preferredWindowEnd: { type: string, format: date-time, nullable: true }
        status: { $ref: "#/components/schemas/LeadStatus" }
    LeadResponse:
      type: object
      required: [lead]
      properties:
        lead: { $ref: "#/components/schemas/Lead" }
    LeadListResponse:
      type: object
      required: [leads]
      properties:
        leads:
          type: array
          items: { $ref: "#/components/schemas/Lead" }
    LeadConvertResponse:
      type: object
      required: [lead, quote]
      properties:
        lead: { $ref: "#/components/schemas/Lead" }
        quote: { $ref: "#/components/schemas/Quote" }

    Property:
      type: object
      required: [id, customerId, address, city, zip]
      properties:
        id: { type: integer }
        customerId: { type: integer }
        address: { type: string }
        city: { type: string }
        zip: { type: string }
        notes: { type: string, nullable: true }

    PropertyWriteBody:
      type: object
      required: [address, city, zip]
      properties:
        address: { type: string, minLength: 1 }
        city: { type: string, minLength: 1 }
        zip: { type: string, minLength: 1 }
        notes: { type: string, nullable: true }

    PropertyResponse:
      type: object
      required: [property]
      properties:
        property: { $ref: "#/components/schemas/Property" }

    PortalRequestOtpInput:
      type: object
      required: [phone]
      properties:
        phone: { type: string, description: "Phone number in any common format; server normalizes to E.164." }
    PortalRequestOtpResult:
      type: object
      required: [ok]
      properties:
        ok: { type: boolean }
        devMode: { type: boolean, description: "True when no Twilio connection is wired; the OTP is then printed to the API server console." }
        devCode: { type: string, nullable: true, description: "Only set in non-production builds when devMode is true. Convenience for local testing." }
        message: { type: string, nullable: true }

    PortalVerifyOtpInput:
      type: object
      required: [phone, code]
      properties:
        phone: { type: string }
        code: { type: string, pattern: "^\\d{6}$" }

    PortalCustomer:
      type: object
      required: [id, fullName]
      properties:
        id: { type: integer }
        fullName: { type: string }
        email: { type: string, nullable: true }
        phoneE164: { type: string, nullable: true }
        billingAddress: { type: string, nullable: true }
    PortalCustomerResponse:
      type: object
      required: [customer]
      properties:
        customer: { $ref: "#/components/schemas/PortalCustomer" }

    PortalPropertiesResponse:
      type: object
      required: [properties]
      properties:
        properties:
          type: array
          items: { $ref: "#/components/schemas/Property" }

    PortalJob:
      type: object
      required: [id, propertyId, status, totalCents, createdAt]
      properties:
        id: { type: integer }
        propertyId: { type: integer }
        crewId: { type: integer, nullable: true }
        status: { type: string }
        scheduledFor: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        totalCents: { type: integer }
        notes: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        propertyAddress: { type: string, nullable: true }
        crewName: { type: string, nullable: true }
    PortalJobsResponse:
      type: object
      required: [upcoming, past, crews]
      properties:
        upcoming:
          type: array
          items: { $ref: "#/components/schemas/PortalJob" }
        past:
          type: array
          items: { $ref: "#/components/schemas/PortalJob" }
        crews:
          type: array
          items:
            type: object
            required: [id, name]
            properties:
              id: { type: integer }
              name: { type: string }

    PortalRequest:
      type: object
      required: [id, customerId, service, status, source, createdAt]
      properties:
        id: { type: integer }
        customerId: { type: integer }
        propertyId: { type: integer, nullable: true }
        service: { $ref: "#/components/schemas/LeadService" }
        notes: { type: string, nullable: true }
        preferredWindowStart: { type: string, format: date-time, nullable: true }
        preferredWindowEnd: { type: string, format: date-time, nullable: true }
        status: { $ref: "#/components/schemas/LeadStatus" }
        source: { $ref: "#/components/schemas/LeadSource" }
        convertedQuoteId: { type: integer, nullable: true }
        createdAt: { type: string, format: date-time }
        propertyAddress: { type: string, nullable: true }
    PortalRequestListResponse:
      type: object
      required: [requests]
      properties:
        requests:
          type: array
          items: { $ref: "#/components/schemas/PortalRequest" }
    PortalRequestResponse:
      type: object
      required: [request]
      properties:
        request: { $ref: "#/components/schemas/PortalRequest" }
    PortalCreateRequestInput:
      type: object
      required: [service]
      properties:
        service: { $ref: "#/components/schemas/LeadService" }
        propertyId: { type: integer, nullable: true }
        notes: { type: string, nullable: true }
        preferredWindowStart: { type: string, format: date-time, nullable: true }
        preferredWindowEnd: { type: string, format: date-time, nullable: true }

    CustomerProfileResponse:
      type: object
      required: [customer, owner, properties, jobs, quotes, invoices, leads, crews, totals]
      properties:
        customer: { $ref: "#/components/schemas/Customer" }
        owner:
          type: object
          nullable: true
          required: [id, fullName, email]
          properties:
            id: { type: integer }
            fullName: { type: string }
            email: { type: string }
        properties:
          type: array
          items: { $ref: "#/components/schemas/Property" }
        jobs:
          type: object
          required: [upcoming, past]
          properties:
            upcoming:
              type: array
              items: { $ref: "#/components/schemas/Job" }
            past:
              type: array
              items: { $ref: "#/components/schemas/Job" }
        quotes:
          type: array
          items: { $ref: "#/components/schemas/Quote" }
        invoices:
          type: array
          items: { $ref: "#/components/schemas/Invoice" }
        leads:
          type: array
          items: { $ref: "#/components/schemas/Lead" }
        crews:
          type: array
          description: Crews referenced by any job in this profile, for client-side name lookup.
          items:
            type: object
            required: [id, name]
            properties:
              id: { type: integer }
              name: { type: string }
        totals:
          type: object
          required: [lifetimeRevenueCents, openQuoteCents, outstandingInvoiceCents, openLeadCount]
          properties:
            lifetimeRevenueCents: { type: integer }
            openQuoteCents: { type: integer }
            outstandingInvoiceCents: { type: integer }
            openLeadCount: { type: integer }
